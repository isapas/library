{% extends 'base.html.twig' %}

{% block title %}Users index{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">
    <h1 class="m-5">La liste des utilisateurs</h1>
    <a class="m-5" href="{{ path('users_index') }}">Retour à la liste des utilisateurs</a>
    <a class="mt-5" href="{{ path('books_index') }}">Retour à la liste des livres</a>    

    <a class="btn btn-success m-5 float-right" href="{{ path('users_new') }}">Create new</a>
    </div>

    <table class="table w-75">
        <thead class="thead-dark text-center">
            <tr>
                <th scope="col" scope="row">Id</th>
                <th scope="col">Name</th>
                <th scope="col">Firstname</th>
                <th scope="col">Adress</th>
                <th scope="col">Code</th>
                <th scope="col">actions</th>
            </tr>
        </thead>
        <tbody class="text-center">
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.name }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.adress }}</td>
                <td>{{ user.code }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('users_show', {'id': user.id}) }}">Détail</a>
                    <a class="btn btn-secondary" href="{{ path('users_edit', {'id': user.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
