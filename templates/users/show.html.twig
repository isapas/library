{% extends 'base.html.twig' %}

{% block title %}Users{% endblock %}

{% block body %}
    <h1>Users</h1>
    <a class="mt-5" href="{{ path('users_index') }}">Retour à la liste des utilisateurs</a>
    <table class="table mb-5 d-flex justify-content-center text-left w-75">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user.name }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ user.adress }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ user.code }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex flex-row bd-highlight mb-3 justify-content-center">
        <a class="btn btn-primary ml-5 mr-5" href="{{ path('users_edit', {'id': user.id}) }}">Modifier</a>
        {{ include('users/_delete_form.html.twig') }}
    </div>
{% endblock %}
